<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- WA-3: daily manager readiness digest. Ships DISABLED
         (active=False), human-first. Enable in Settings then Technical
         then Scheduled Actions when ready. noupdate so a later upgrade
         keeps a manual toggle. -->
    <data noupdate="1">
        <record id="ir_cron_wa3_readiness_digest" model="ir.cron">
            <field name="name">WA-3: Daily readiness digest</field>
            <field name="model_id"
                   ref="neon_crew_comms.model_neon_readiness_digest"/>
            <field name="state">code</field>
            <field name="code">model._cron_send()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="False"/>
        </record>
    </data>
</odoo>
