# Drop custom Odoo modules into this directory.
# Each module should live in its own subfolder and contain an __manifest__.py file.
#
# This directory is mounted into the container at /mnt/extra-addons.
# After adding or removing a module, restart the Odoo container:
#   docker compose restart odoo
#
# Then activate developer mode in Odoo and use "Update Apps List"
# before installing the new module.
