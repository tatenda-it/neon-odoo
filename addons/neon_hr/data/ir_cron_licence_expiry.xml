<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Daily scan: raise/refresh an Action Centre item for every driver
         licence expiring within the configurable lead window (and for
         already-expired licences); also nudges licence + competency
         stored states. nextcall tomorrow 02:20 so install/smoke don't
         spawn items mid-process. -->
    <record id="ir_cron_licence_expiry" model="ir.cron">
        <field name="name">Neon HR: Driver Licence Expiry Scan</field>
        <field name="model_id" ref="model_neon_hr_licence"/>
        <field name="state">code</field>
        <field name="code">model._cron_licence_expiry_scan()</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="active" eval="True"/>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="doall" eval="False"/>
        <field name="nextcall"
               eval="(DateTime.now().replace(hour=2, minute=20, second=0, microsecond=0) + relativedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
    </record>
</odoo>
